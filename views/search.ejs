<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>機械検索 | 佐渡農業機械レンタル</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('header', { pageTitle: '機械検索', pageDescription: 'お探しの農業機械を検索できます' }) %>
  
  <div class="container">
    
    <div class="search-container">
      <form action="/search" method="GET" class="search-form">
        <input type="text" name="keyword" placeholder="機械名を入力" value="<%= keyword %>" class="search-input" required>
        <button type="submit" class="search-button">検索</button>
      </form>
    </div>
    
    <% if (keyword) { %>
      <h2 class="search-results-title">"<%= keyword %>"の検索結果</h2>
      
      <% if (machines.length > 0) { %>
        <div class="machines">
          <% machines.forEach(function(machine) { %>
            <%- include('machine-card', { machine: machine }) %>
          <% }); %>
        </div>
      <% } else { %>
        <div class="no-results">
          <p>「<%= keyword %>」に一致する機械は見つかりませんでした。</p>
          <p>別のキーワードで検索するか、<a href="/">すべての機械</a>をご覧ください。</p>
        </div>
      <% } %>
    <% } %>
  </div>
  
  <%- include('footer') %>

</body>
</html>